syntax = "proto3";

package block;

message Block {
  int32 version = 1;
  int32 timestamp = 2;
  string parent = 3;
}

message Row {
  repeated string primary_key = 1;
  repeated string subsidiary = 2;
}

message Table {
  repeated Row rows = 1;
}

message PreviousState {
  map<string, Table> tables = 1;
}
