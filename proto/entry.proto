syntax = "proto3";

package entry;

import "cell.proto";

// Entry represents a row in a table, split into key and value components.
message Entry {
  // The values that make up the composite primary key.
  repeated cell.Value key = 1;
  // The subsidiary (non-key) values.
  repeated cell.Value value = 2;
}
