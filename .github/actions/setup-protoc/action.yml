name: Setup protoc
description: Install protobuf compiler from official releases

runs:
  using: composite
  steps:
    - name: Install protobuf compiler
      shell: bash
      run: bash .github/scripts/setup-protoc.sh
